# Conditional CycleGAN — Python dependencies
# Install with:  pip install -r requirements.txt
#
# Tested with Python 3.10+
# CUDA 11.8+ recommended for GPU training (install matching torch from pytorch.org)

# ── Core deep learning ────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0

# ── ArcFace identity extractor (InceptionResnetV1, VGGFace2 pretrained) ───────
facenet-pytorch>=2.5.3

# ── Image I/O ─────────────────────────────────────────────────────────────────
Pillow>=9.5.0
opencv-python>=4.8.0   # used by facenet-pytorch internals

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy>=1.24.0
tqdm>=4.66.0
